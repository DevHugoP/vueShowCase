import { CreateOrderStateDeliveryContact } from './types'

const deliveryContact = (state: CreateOrderStateDeliveryContact): CreateOrderStateDeliveryContact =>
  state

export default { deliveryContact }
